Finishing a Skill doesn't mean you can trust it works. You have to test it. I picked a few pages on the docs site that I'd never looked at before, then threw a handful of questions at the newly written [`claude-code-docs`](../../.claude/skills/claude-code-docs/SKILL.md) that had nothing to do with Quickstart. Every time, it used the index to find the right page, fetched it, and gave a well grounded answer.
