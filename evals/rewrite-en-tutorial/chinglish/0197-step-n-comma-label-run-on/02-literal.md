**Step 9**, learn how to turn text into math vectors you can compare. Everything later about retrieval sits on top of this.

**Step 10**, last lesson you computed cosine by hand, this lesson you hand it off to a real vector store (fast queries, filtering support, persistence to disk).

**Step 11**, you go from "a few hardcoded strings" to "read real files and split them into chunks of the right size", which is the data-prep step a real RAG system needs.

**Step 12**, the RAG theme wraps, and you can build a real "QA over my own docs" app.
