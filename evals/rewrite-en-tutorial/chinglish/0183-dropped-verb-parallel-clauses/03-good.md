Why split it into two files? In one line: templates go into git, real values don't. Having the template means whoever clones the project next doesn't have to guess which variables they need. The real values stay on your machine, so nothing leaks. Almost everyone in open source follows this convention. The next time you see a project's `.env.example`, your instinct should be to copy it to `.env` and fill it in.
