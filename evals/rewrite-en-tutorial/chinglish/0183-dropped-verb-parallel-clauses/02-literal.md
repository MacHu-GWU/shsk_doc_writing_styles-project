Why split it into two files? One sentence: template into git, real values not into git. The template means the next person to clone doesn't have to guess which variables exist. The real values stay local, no leaks. Pretty much the entire open source world follows this convention. Any time you see a project's `.env.example`, you should instinctively think "cp it to .env and fill it in."
