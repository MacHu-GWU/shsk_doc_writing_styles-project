Four of them. First, extending the split rules: by share count, by ratio, letting someone sit out a specific expense, and figuring out how to fit all that naturally into the Split model. Second, the minimum transfers algorithm: is the greedy solution good enough, or is it worth chasing the theoretical optimum? Third, amount precision and rounding: when a split doesn't divide evenly, who ends up with that extra cent? Fourth, read consistency when several people edit the same group's ledger at once.
