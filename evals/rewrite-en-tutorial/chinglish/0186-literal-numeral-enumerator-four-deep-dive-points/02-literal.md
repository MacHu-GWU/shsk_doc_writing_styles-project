Four of them. One, extending the splitting rules — by shares, by ratio, someone not participating in a given expense — how to accommodate that naturally in the Split model. Two, the minimum-transfers algorithm — is the greedy solution good enough, or should we pursue the theoretical optimum? Three, amount precision and rounding — when an even split doesn't divide evenly, who eats that last cent? Four, read consistency when the same group's ledger is being edited concurrently.
